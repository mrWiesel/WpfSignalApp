<Page x:Class="WpfSignalApp.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Black" Title="Signal Terminal" FontFamily="Consolas">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" BorderBrush="DarkGray" BorderThickness="2" Margin="2">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Ellipse Width="100" Height="100" Stroke="White" StrokeThickness="2" Margin="10"/>
                <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                    <TextBlock Text="Polarity filter:" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                    <TextBlock x:Name="TxtPolTarget"  Text="Target offset: --- deg"  Foreground="White"/>
                    <TextBlock x:Name="TxtPolCurrent" Text="Current offset: 0.00 deg" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" BorderBrush="DarkGray" BorderThickness="2" Margin="2">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Ellipse Width="100" Height="100" Stroke="White" StrokeThickness="2" Margin="10"/>
                <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                    <TextBlock Text="Frequency filter:" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                    <TextBlock x:Name="TxtFreqTarget"  Text="Target offset: --- MHz"  Foreground="White"/>
                    <TextBlock x:Name="TxtFreqCurrent" Text="Current offset: 0.00 MHz" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="0" Grid.Column="1" BorderBrush="DarkGray" BorderThickness="2" Margin="2">
            <TextBlock x:Name="TxtSignalStatus" Text="NO SIGNAL" Foreground="Red"
                       FontSize="36" FontWeight="Bold"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" BorderBrush="DarkGray" BorderThickness="2" Margin="2">
            <StackPanel Margin="10" VerticalAlignment="Center">
                <TextBlock Text="Detector status: Active" Foreground="LimeGreen"/>
                <TextBlock x:Name="TxtObject"  Text="Object: none"         Foreground="White"/>
                <TextBlock x:Name="TxtQuality" Text="Signal quality: 0%"   Foreground="White"/>
                <TextBlock Text="Efficiency: 0.000 B\s"       Foreground="Yellow"/>
                <TextBlock Text="Energy consumption: 100.00%" Foreground="DeepSkyBlue"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="INITIALIZE SCANNER"       Width="150" Height="30" Margin="5"
                    Background="#333" Foreground="White" Click="BtnCatch_Click"/>
            <Button x:Name="BtnRepeat"
                    Content="AUTO-CALIBRATE (REPEAT)"  Width="180" Height="30" Margin="5"
                    Background="#333" Foreground="White" IsEnabled="False" Click="BtnRepeat_Click"/>
        </StackPanel>
    </Grid>
</Page>
