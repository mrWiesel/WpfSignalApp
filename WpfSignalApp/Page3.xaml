<Page x:Class="WpfSignalApp.Page3"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource ThemePageBg}"
      FontFamily="Segoe UI"
      Title="Signal Picture">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Локалізований заголовок — оновлюється з code-behind -->
        <TextBlock x:Name="TbTitle"
                   Grid.Row="0"
                   FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource ThemeText}"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="1"
                Background="{DynamicResource ThemeCard}"
                BorderBrush="{DynamicResource ThemeBorder}"
                BorderThickness="1.5" CornerRadius="8"
                Margin="0,0,0,8">
            <Image x:Name="MyImage" Stretch="Uniform" Margin="4"/>
        </Border>

        <Border Grid.Row="2"
                Background="{DynamicResource ThemeCard}"
                BorderBrush="{DynamicResource ThemeBorder}"
                BorderThickness="1.5" CornerRadius="8">
            <Canvas x:Name="SignalCanvas" Background="Transparent">
                <Polyline x:Name="waveLine" Stroke="Lime" StrokeThickness="2"/>
            </Canvas>
        </Border>
    </Grid>
</Page>
